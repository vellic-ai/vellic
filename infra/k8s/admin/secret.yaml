# STUB — populate via external-secrets or sealed-secrets before deploying
apiVersion: v1
kind: Secret
metadata:
  name: admin-secrets
  namespace: vellic
  labels:
    app.kubernetes.io/name: vellic-admin
    app.kubernetes.io/component: admin
type: Opaque
stringData:
  DATABASE_URL: "postgresql://vellic:CHANGE_ME@postgres:5432/vellic"
  REDIS_URL: "redis://redis:6379"
