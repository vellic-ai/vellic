# CI overlay: probe nginx directly instead of the /health proxy to admin.
services:
  frontend:
    healthcheck:
      test: ["CMD-SHELL", "test -f /var/run/nginx.pid && kill -0 $(cat /var/run/nginx.pid) 2>/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
