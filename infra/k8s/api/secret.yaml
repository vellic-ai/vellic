# STUB — populate values via external-secrets or sealed-secrets before deploying
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: vellic
  labels:
    app.kubernetes.io/name: vellic-api
    app.kubernetes.io/component: api
type: Opaque
stringData:
  DATABASE_URL: "postgresql://vellic:CHANGE_ME@postgres:5432/vellic"
  REDIS_URL: "redis://redis:6379"
  GITHUB_WEBHOOK_SECRET: "CHANGE_ME"
